
command to call DB setup schema -> \i C:/RUST_PROJECTS/movie_site_folder/tide_test/db_setup/setup_db.sql;
command to call DB erase schema -> \i C:/RUST_PROJECTS/movie_site_folder/tide_test/db_setup/clear_db.sql;
command to enter the database -> psql -d movie_site_test -U postgres
password for DB -> password
command to build the web assembly -> cargo watch -s "wasm-pack build --target web --out-name wasm --out-dir ./static" 
command to run the wasm server -> miniserve ./static --index index.html


https://github.com/alexc-hollywood/imdb-extractors
DO WE WANT TO SWITCH APIs? THERE ARE SOME OTHERS THAT MIGHT BE BETTER...

MP LOGIN INFO

CHANGED TO AN SQLITE DB
sqlite3 should be standard on linux.

sqlite3 movie_site.db
CREATES NEW DATABASE (OR OPENS IT) IN THE CURRENT DIRECTORY.

sqlite3 movie_site.db < db_setup/clear_db.sql
CLEARS THE DATABASE

sqlite3 movie_site.db < db_setup/setup_db.sql
SETS UP THE DB


TOOLS:

cargo install cargo-watch
cargo install cargo-make
cargo install miniserve


COMMAND TO SERVE THE CLIPBOARD
miniserve clipboard.txt --port 8001





